import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
    typography: {
        fontFamily: "Muli"
    },
    palette: {
        primary: {
            main: "#440099"
        },
        secondary: {
            main: "#ffa857"
        }
    }
})

export default theme;